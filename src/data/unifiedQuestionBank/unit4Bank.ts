import { TraceableQuestion } from '../../data/expandedQuestionBank';
import { unit4CoreBank } from '../unit4CoreBank';

/**
 * Unit 4 Question Bank: Trial Balance and Error Correction (ميزان المراجعة وتصحيح الأخطاء)
 * 40 Traceable Questions faithfully transcribed exclusively from the authentic Unit 4 curriculum source.
 */
export const unit4Bank: TraceableQuestion[] = unit4CoreBank;
